# Benchmarks: Rust bbolt vs Go bbolt v1.5.0

Apples-to-apples workloads on the **same cloud VM**, same options, same on-disk path.

| Side | Implementation |
| --- | --- |
| Go | `go.etcd.io/bbolt` **v1.5.0** (`benches/go`) |
| Rust | this crate, `--release` (`bench_compare` binary) |

## Environment (captured run)

| Item | Value |
| --- | --- |
| Date (UTC) | 2026-08-27 |
| CPUs | 4 (`nproc`) |
| RAM | ~16 GiB |
| Disk path | `/workspace/bench-data` on **overlayfs** backed by the VM disk image (**not** tmpfs / `shm`) |
| Page size | 4096 |
| Freelist | **array** (both) |
| `NoSync` / `NoGrowSync` | false unless row labeled `_nosync` |
| Fill percent | library defaults (0.5) |
| Rustc | 1.83.0 |
| Go | go1.24.0 host / `GOTOOLCHAIN=auto` pulls toolchain required by bbolt v1.5.0 |
| Method | 1 warmup + **5 trials**, **median** by `elapsed_ns` |

## How to run

```bash
# Builds both harnesses, runs all workloads, writes bench-data/results.tsv
./benches/run_compare.sh

# Or single shot:
cargo build --release --bin bench_compare
(cd benches/go && GOTOOLCHAIN=auto go build -o go-bench .)

./benches/go/go-bench -dir /tmp/g -workload seq_put -n 100000 -key-size 8 -value-size 32
./target/release/bench_compare --dir /tmp/r --workload seq_put --n 100000 --key-size 8 --value-size 32
```

Optional env: `TRIALS=5`, `WARMUP=1`, `BENCH_DIR=/path/to/dir`.

Workloads time **only the measured phase** (prepare/load for gets/scans/deletes is outside the timer). Keys are 8-byte big-endian counters (left-padded to `key-size`); values are deterministic bytes of `value-size`.

## Results (ops/sec, median)

Ratio = **Rust / Go** (higher is better for Rust; `< 1` means Rust is slower).

| Workload | Go ops/s | Rust ops/s | Ratio | Go ms | Rust ms | Go file B | Rust file B | Notes |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| seq_put | 911,223 | 24,452 | **0.027** | 110 | 4090 | 16,777,216 | 11,542,528 | 100k keys, 32 B values, **fsync on** |
| random_get | 2,400,984 | 1,287,532 | **0.536** | 42 | 78 | 16,777,216 | 11,542,528 | 100k random gets after load |
| cursor_scan | 72,984,337 | 2,609,622 | **0.036** | 1.4 | 38 | 16,777,216 | 11,542,528 | full forward cursor |
| random_put | 4,778 | 12,271 | **2.57** | 20,929 | 8,149 | 16,777,216 | 11,542,528 | 100k scrambled keys, one txn |
| deletes | 3,019,011 | 1,280,992 | **0.424** | 17 | 39 | 16,777,216 | 11,563,008 | delete 50% of 100k (50k ops) |
| one_large_tx | 859,812 | 225,509 | **0.262** | 12 | 44 | 2,097,152 | 1,179,648 | 10k puts, one Update |
| many_small_tx | 11,547 | 9,656 | **0.836** | 866 | 1,036 | 2,097,152 | 1,191,936 | 10k Updates × 1 put |
| large_value | 26,653 | 12,540 | **0.470** | 375 | 797 | 139,927,552 | 123,150,336 | 10k × 10 KiB values |
| seq_put_nosync | 1,046,445 | 23,862 | **0.023** | 96 | 4,191 | 16,777,216 | 11,542,528 | same as seq_put with **NoSync=true** |

Raw TSV from the harness: `bench-data/results.tsv` (regenerated by the script; not required to commit bulky DB files).

## Reading the numbers

- **Sequential bulk put is where Rust lags most** (~40×). The `seq_put_nosync` row is nearly as slow as fsync-on seq_put, so the gap is **not** explained by forgetting `NoSync` on one side — both sides use the same flag; a single large transaction only fsyncs at commit.
- **Random put** favors Rust (~2.6×) on this machine for the same 100k one-txn load.
- **Reads**: random get ~0.54×; full cursor scan ~0.04× (Go is much faster at tight iteration).
- **many_small_tx** (one key per Update, fsync each commit) is closer (~0.84×) because both pay commit/fsync overhead.
- **File sizes differ** after the same logical load (Go often lands on larger power-of-two growth steps). That is allocation/mmap growth policy, not a claim of different user data; cross-impl oracle tests cover data interchange separately.

## Fairness notes

- Same N, key/value sizes, page size 4096, freelist **array**, default fill percent.
- Neither side was specially tuned for this run.
- `NoSync` appears only on the explicitly labeled `seq_put_nosync` row.
- Disk is the workspace overlay (VM disk), not `/dev/shm`.
